# Dependencias do projeto Brasileirao (Sofascore scraper + modelagem).
# Instale com:  pip install -r requirements.txt
requests==2.31.0
mysql-connector-python==9.7.0
python-dotenv==1.0.0

# Modelagem (baseline XGBoost / futuro GNN)
pandas>=2.0
numpy>=1.26
pyarrow>=15.0
scikit-learn>=1.4
xgboost>=2.0
matplotlib>=3.8
